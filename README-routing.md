# Portfolio navigation

The main navigation uses dedicated lightweight placeholder pages for About, Skills, Projects, Experience, Resume and Contact. The header collapses into an accessible hamburger menu on small screens.
